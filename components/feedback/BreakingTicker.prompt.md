One-line: The full-width scrolling breaking-news marquee anchored under the header, with an angled vermillion "Breaking" tag and a pulsing live dot.

```jsx
<BreakingTicker label="Breaking" items={[
  'Cabinet clears new IT policy',
  'Heavy rain alert for coastal districts',
  'Sensex closes 400 pts higher',
]} />
```

Items scroll right-to-left on a seamless loop (speed = `--ticker-speed`). Ink background, red tag.
