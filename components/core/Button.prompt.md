One-line: The brand call-to-action button — vermillion fill by default; use for follow, subscribe, read-more, and primary form actions.

```jsx
<Button variant="primary" size="md">Follow</Button>
<Button variant="soft" size="sm">Read more</Button>
<Button variant="secondary" icon={<span>↻</span>}>Refresh feed</Button>
```

Variants: `primary` (vermillion, default), `secondary` (white/bordered), `ghost`, `soft` (red tint), `inverse` (ink). Sizes `sm|md|lg`. Supports `icon`/`iconRight`, `block`, `disabled`. Pill-shaped, Anek display font.
