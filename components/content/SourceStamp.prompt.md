One-line: The trust line beneath every headline — source, hyperlocal location, relative time, plus verified check and an "AI rewrite" transparency mark.

```jsx
<SourceStamp source="Eenadu" location="Vijayawada" time="12 min" verified aiRewritten />
```

Renders `source · location · time` with dot separators. `verified` adds a blue check; `aiRewritten` appends the transparency flag.
