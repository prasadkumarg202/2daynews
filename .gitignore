# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Historical design-system snapshots (~21MB). The unpacked current state
# in components/, tokens/, and ui_kits/ supersedes these; git history is
# the versioning mechanism now.
*.zip

# Dependencies (if you later add a build)
node_modules/
dist/
.next/

# Local env
.env
.env.local

# Compiler-generated design-system artifacts are safe to commit,
# but uncomment to exclude if you regenerate them in CI:
# _ds_bundle.js
# _ds_manifest.json
# _adherence.oxlintrc.json
